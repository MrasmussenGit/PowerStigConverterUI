<Window x:Class="PowerStigConverterUI.ConvertStigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Convert STIG"
        Height="700" Width="1000"
        Background="{StaticResource BackgroundLightBrush}"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource HeaderBorderStyle}">
            <TextBlock Text="Convert STIG" Style="{StaticResource HeaderTitleStyle}"/>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Advanced: Module selection de-emphasized (moved above Input and Output) -->
        <Expander Grid.Row="0" IsExpanded="False" Header="Advanced (PowerStig module)" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="Module path:" Style="{StaticResource LabelTextStyle}"/>
                <TextBox x:Name="ModulePathTextBox" Grid.Column="1" Margin="0,0,8,0" MinWidth="500"/>
                <Button Grid.Column="2" Content="Change..." Style="{StaticResource StandardButtonStyle}" Click="BrowseModule_Click"/>
            </Grid>
        </Expander>

        <!-- Primary inputs: XCCDF + Output grouped -->
        <GroupBox Header="Input and Output" Grid.Row="1" Margin="0,0,0,8">
            <Grid Margin="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- XCCDF -->
                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="XCCDF or ZIP file:" Style="{StaticResource LabelTextStyle}"/>
                <TextBox x:Name="XccdfPathTextBox" Grid.Row="0" Grid.Column="1" Margin="0,0,8,4" MinWidth="500"/>
                <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Style="{StaticResource StandardButtonStyle}" Click="BrowseXccdf_Click" Margin="0,0,0,4"/>

                <!-- Output -->
                <TextBlock Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="Output folder:" Style="{StaticResource LabelTextStyle}"/>
                <TextBox x:Name="DestinationTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,8,0" MinWidth="500"/>
                <Button Grid.Row="1" Grid.Column="2" Content="Browse..." Style="{StaticResource StandardButtonStyle}" Click="BrowseDestination_Click"/>
            </Grid>
        </GroupBox>

        <!-- Options -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
            <CheckBox x:Name="CreateOrgSettingsCheckBox" Content="Create org settings file" IsChecked="True" Margin="0,0,12,0"/>
            <CheckBox x:Name="SkipCompareCheckBox" Content="Skip compare step" Margin="0,0,12,0"/>
            <CheckBox x:Name="AddFailedRulesToLogCheckBox" Content="Add failed rules to log file" IsChecked="True"/>
        </StackPanel>

        <!-- Info / Logs -->
        <GroupBox Grid.Row="3">
            <GroupBox.Header>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Messages" FontWeight="SemiBold" Margin="0,0,8,0"/>
                    <TextBlock Text="(Double-click a rule ID to see details)" Style="{StaticResource HintTextStyle}"/>
                </StackPanel>
            </GroupBox.Header>
            <RichTextBox x:Name="InfoRichTextBox"
                 Margin="8"
                 IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 PreviewMouseDoubleClick="InfoRichTextBox_PreviewMouseDoubleClick"/>
        </GroupBox>

        <!-- Bottom bar: Busy left, Actions right -->
        <Grid Grid.Row="4" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Busy indicator (left aligned) -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <ProgressBar x:Name="BusyProgressBar"
                             Width="180"
                             Height="16"
                             IsIndeterminate="True"
                             Visibility="Collapsed"/>
                <TextBlock x:Name="BusyStatusText"
                           Margin="8,0,0,0"
                           Foreground="DarkSlateGray"
                           VerticalAlignment="Center"
                           Visibility="Collapsed"
                           Text="Converting…"/>
            </StackPanel>

            <!-- Actions (right aligned) -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="ConvertButton" Content="Convert" Style="{StaticResource PrimaryButtonStyle}" Width="120" Margin="0,0,8,0" Click="ConvertButton_Click"/>
                <Button x:Name="ViewReportButton" 
                        Content="View Report" 
                        Style="{StaticResource StandardButtonStyle}"
                        Width="120" 
                        Margin="0,0,8,0" 
                        Click="ViewReportButton_Click"
                        Visibility="Collapsed"
                        IsEnabled="False"/>
                <Button x:Name="CloseButton" Content="Close" Style="{StaticResource SecondaryButtonStyle}" Width="120" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
        </Grid>
    </Grid>
</Window>